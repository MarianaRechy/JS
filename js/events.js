//!Eventos
/*
Podemos capturar y reaccionar a esos eventos,
manipularlos a nuestro favor.

//!Tipos de eventos
    Automaticos
    Generados por ususarios

//! Online events

const funEvent = () =>{
    console.log ("bbbbbbbb")
}



//on error

document.getElementById("myImg").onerror = function() {myFunction()};

function myFunction() {
  document.getElementById("demo").innerHTML = "The image could not be loaded.";
}

//!EventListerner
// Es un metodo de los elementos del DOM

let btn = document.querySelector(".btn")

btn.addEventListener("click", funEvent) //no se pone el on porque no esta en linea

//! event handlers

//! event object
hace referencia al objeto que se crea cuando se captura un evento



REPASO

Destructuracion de objetos
*Acceder a través de una variable a un objeto 
let { a, b, c} =objectPrueba
a:nombre --> con dos puntos asigna otro nombre a la vaiable.Acceder


//!Callback: funcion que pasamos como un parametro. El llamado a una funcion cuando se ejecuta.
// Coleccion INDEXADA tiene un indice llave o key, empieza a contar a partir de 0.

/! 1 Menejo de envento en el boton "show Card"
// Pasos
// 1. Seleccionar el holder de la card
// 2. seleccionar quien detona el evento
// 3. agregar un listener al detonador
// 4. insertar html al holder
// 5. Opcional insertar elemento en el card para eliminarla

https://getbootstrap.com/docs/5.2/components/card/#example

https://getbootstrap.com/docs/5.2/components/card/#example



//! Event propagation
  // ! Bubbling
  // ! Capturing

//! Preventing the Default Action

.addEventListener

.removeEventListener( click, callback, true)


*/
