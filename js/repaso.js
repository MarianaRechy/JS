/*
MAP ITERA Y RETORNA UN NUEVO ARREGLO, TRASNFORMA Y DA UNA NUEVA SALIDA. RETORNA LA MISMA CANTIDAD DE ELEMENTOS.

FILTER REGRSA LA CANTIDAD DE ELEMENTOS QUE CUMPLAN LA CONDICION

Array de 5 frutas
// se le aplica map . Resulta de un array de 5 nuevos elementos.

FILTER

var frutasFiltradas = frutas.filter(funcion(fruta)){
    console.log (`La fruta a filtrar es #${fruta}`)
    let ultimaLetra = fruta.charAt(fruta.length-1)
    console.log (`El resultado del charAt #${ultimaletra}`)
    let cumpleConLaCondicion = ultimaletra == "e";
    console.log (`E #${ultimaletra}`)
}

//reduce retorna la lista final del acumulador, aporta mas al acumulador
